overwrite: true
schema: "http://localhost:3000/graphql"
documents: null
generates:
  src/generatedTypes/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
